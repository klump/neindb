<div class="page-header">
  <h1>Assets <small>Overview</small></h1>
</div>

<table class="table table-condensed table-striped">
  <thead>
    <tr>
      <th>Serial</th>
      <th>Product name</th>
      <th>Type</th>
    </tr>
  </thead>

  <tbody>
    <% @assets.each do |asset| %>
      <tr>
        <td><%= link_to asset.name, asset %></td>
        <td><%= asset.product_name %></td>
        <td><%= asset.type %></td>
      </tr>
    <% end %>
  </tbody>
</table>
